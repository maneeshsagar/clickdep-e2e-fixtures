services:
  web:
    build: ./web
    expose: ["80"]
    x-clickdep-role: frontend
  api:
    build: ./api
    expose: ["3000"]
    x-clickdep-role: backend
    environment:
      E2E_FROM_COMPOSE: hello-from-compose
